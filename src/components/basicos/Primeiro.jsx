import React from 'react'

//Componentes funcionais, que são baseados em função
export default function Primeiro(){
    const msg = 'Seja bem vindo(a)! ao curso de REACT'
    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{ msg }</p>
        </div>
    )
}
